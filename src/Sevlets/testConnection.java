package Sevlets;

import java.io.IOException;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.SQLException;




public class testConnection {

	public static void main(String[] args) throws ClassNotFoundException, SQLException,  IOException {
		/*String nombre= ("Noob");
		String edad = ("edad");
		String carrera = "estudiante";
		String ageCarrera="dos";
		String numTel = ("telefono");
		String direccion =("direccion");
		String email = ("lololol");

		baseDatos hola = new baseDatos();
		System.out.println(hola.conectar());
		Connection conectar = hola.conectar();
		String sql = "INSERT INTO datos (Nombre, Apellido, Edad, Direccion, telefono, carrera, sexo) VALUES (?,?,?,?,?,?,?)";
		PreparedStatement prepare = conectar.prepareStatement(sql);
		prepare.setString(1, nombre);
		prepare.setString(2, email);
		prepare.setString(3, edad);
		prepare.setString(4, direccion);
		prepare.setString(5, numTel);
		prepare.setString(6, carrera);
		prepare.setString(7, ageCarrera);
		prepare.executeUpdate();*/
		
		

	}

}
